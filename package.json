{
    "scripts": {
        "heroku-prebuild": "cd frontend && npm install && npm run build && mv -vf build/* ../backend/public/" ,
        "start": "cd backend && npm install && node server.js"
    }
}
